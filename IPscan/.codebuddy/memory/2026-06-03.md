# 2026-06-03

## 修复内网检测工具外网识别问题
- **问题**：`check_ip_has_internet` 在无匹配本机网卡时直接返回 False，导致无法正确识别能连外网的 IP
- **修复内容**：
  1. 扩展外网探测目标池：TCP 6→11 个目标，HTTP 2→4 个，DNS 3→6 个（多运营商冗余）
  2. 重写 `_self_test_internet`：并行 TCP → 串联重试两轮机制，HTTP/DNS 并行轮询
  3. 重写 `_test_internet_from_interface`：绑包 TCP → 纯 TCP 回退 → 自检兜底三层防护
  4. 新增 `_ping_gateway` 网关连通性检测
  5. 重写 `check_ip_has_internet`：网卡匹配 → 网关检测 → 自检补正三层判定
  6. 扫描范围从 .0~.254 改为 .1~.254（跳过网络地址）
  7. 优化日志输出和报告导出内容
